[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "test-python-release"
version = "0.1.1"
description = "Test project for agent-release"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
